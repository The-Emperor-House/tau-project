import ProjectCards from '../home/components/ProjectCards';

export default function ProjectsPage() {
  return (
    <div style={{ paddingTop: "var(--page-top)", backgroundColor: "var(--bg-color, #404040)" }}>
      <ProjectCards />
    </div>
  );
}
